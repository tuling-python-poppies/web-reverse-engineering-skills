# Case live-reference archive

Read-only historical implementations restored from git `f19f82c~1` before the offline-only case rewrite.

These files are study references only. They are not delivery code, not case entry points, not hash-bound case assets, and must not be imported by active `references/cases/**` code.

Active case entries remain offline-only. Final live egress belongs to a task project `main.py` through the `python-collector` delivery Provider under a validated work order.
