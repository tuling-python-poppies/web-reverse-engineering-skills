# Server-JS Cookie Bootstrap Playbook

Thin entry only. Prefer the canonical challenge playbook on first load.

## When

- same endpoint returns non-final status (often `202`) with executable JS, then data after a derived cookie/token
- a page refresh function renews cookies/params plus in-memory timestamp slots

## Next read (exactly one)

`references/challenge-state-envelope-playbook.md` — **Server-JS cookie bootstrap** section. Cookie writer still unknown → then `cookie-provenance-playbook.md` as a later expansion. Passive key/config only → `public-bootstrap-envelope-playbook.md`.
